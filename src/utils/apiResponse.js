class apiResponse {
    constructor(statusCode, data, message, errors) {
        statusCode = this.statusCode,
            data = this.data,
            message = this.message

    }
}

export { apiResponse }